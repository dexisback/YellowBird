services:
  postgres:
    ...

  redis:
    image: redis:7-alpine
    container_name: yellowbird-redis
    restart: unless-stopped

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:




  